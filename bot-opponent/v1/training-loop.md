# Training loop — Bot V1/Renekton

Protocolo dos ciclos de heartbeat (~30 min) do treinamento autônomo do V1.
Supervisor: agente principal do Kimi. Execução: subagents. Revisão: adversarial
a cada ciclo.

## Missão

Fazer o V1/Renekton jogar bem o suficiente para derrotar o baseline de forma
consistente e, depois, pressionar humanos — sem nunca trapacear as regras da
Match (D3).

## Protocolo de cada ciclo

1. **Estado**: rodar `npm test` na raiz; se vermelho, o ciclo é só reparo.
2. **Leitura**: reler a seção "Log de ciclos" abaixo e escolher o próximo
   item do backlog não marcado como feito.
3. **Execução**: delegar a implementação a um subagent coder com briefing
   completo (objetivo, arquivos, contratos, restrições). Trabalho pequeno e
   verificável por ciclo — um item, não três.
4. **Revisão adversarial**: revisar o diff como revisor hostil (regressões,
   acoplamento com `game/`, bot trapaceando regras, teste que não exercita o
   caminho novo). Quando disponível, invocar a skill `codex-mode` para uma
   segunda revisão independente; registrar se ela rodou ou falhou.
5. **Registro**: atualizar o "Log de ciclos" com data, item, outcome, métricas
   e próximo passo. Atualizar docs do módulo se algum contrato mudou.

## Restrições duras

- Nunca Playwright headless (`STOP-HEADLESS-PLAYWRIGHT.md`).
- `npm test` verde ao fim de cada ciclo; diff mínimo; sem refator oportunista.
- Bot nunca escreve estado da Match; só emite intenções (D3/D9).
- Fury, cooldowns e vida vêm do WorldView, nunca de memória do bot.
- Geografia: máximo 3 níveis; sem pastas novas fora de `bot-opponent/v1/`.

## Backlog ordenado

- [x] B1 — Kit no WorldView: confirmado que o WorldView já expõe cooldowns
      flat (`qCooldown`…`rCooldown`), `fury`, `skillsUnlocked` (array 0=Q…3=R),
      `renektonDashRecast` e `renektonDominus`; `skillReady` alinhado ao
      formato real, incluindo janela de recast do E ("Dice") e guarda de
      Dominus ativo. Formato documentado no header de `renekton-skills.mjs`.
- [x] B2 — `Game.updateBot()` encaminha `intent.skill` para o mesmo
      entrypoint do input humano (`castAbility`); definir mira de skills
      direcionais (facing preparado ou campo explícito).
      Entregue: mapeamento q/w/e/r → slots 0–3 com `Object.hasOwn`, bundle
      `load-v1-bot.js` gerado no build, fallback baseline sem o bundle; mira
      segue `lastDx/lastDz` (movimento do bot). Revisão adversarial Codex
      aplicada no ciclo 2b.
- [x] B3 — Harness CPU-vs-CPU headless em Node: N partidas V1 vs baseline
      com RNG determinístico; reporta win rate e métricas. Sem browser.
      Entregue em `v1/run-cpu-duels.mjs` + `v1/verify-cpu-duels.test.mjs`
      (P1 pilotado por imitação de teclado WASD, sem costura em `game/`).
- [x] B4 — Mapa de perigo temporal: por célula, quando fica mortal e quando
      volta a ser segura (derivado de bombas+fuses); função pura e testada.
      Entregue no ciclo 7 em `v1/danger-timeline.mjs` (envelope
      `{ deadlyFrom, deadlyUntil }` por célula, blast 0,58s, cadeia com
      atraso de 1 frame — valores verificados em `game/`).
- [x] B5 — Escape tempo-expandido: BFS com dimensão temporal +
      `hasEscape(bomba hipotética)`; bomba só com rota de fuga.
      Entregue no ciclo 7: `escapePlan` (Dijkstra em (célula, tempo) com
      espera em célula segura e fallback "menos pior") e
      `hasTemporalBombEscape` (prova de fuga temporal para todo plant —
      open-route e veto de plants do baseline).
- [x] B6 — Recast do E e combos Renekton (Slice→Dice, engage→W→Q→saída)
      usando `renekton-memory`. Entregue nos ciclos 8 (combos + Dominus + mira)
      e 9 (correções da revisão adversarial: ordem Dice, facing de escape,
      prazo do aimPending, reconciliação de cast, W só sem perigo).
- [x] B7 — Modelo do rival: memória entre rounds de hábitos (rotas de fuga,
      onde planta); usar no posicionamento de bomba. Entregue no ciclo 10
      em `v1/read-rival.mjs` (heat map + rotas de fuga + hábito de bomba,
      interceptação e plant corta-fuga); contra o baseline o modelo fica
      inerte por dado real (rival travado não foge) — valor esperado no
      self-play do B8.
- [x] B8 — Personalidade V1: pesos próprios (agressão, risco, ganância de
      pickup) tunados via self-play do B3.

## Métricas de referência (a partir do B3)

- Win rate V1 vs baseline em 100 partidas (alvo inicial: > 60%).
- Sobrevivência à própria primeira bomba (> 90%).
- Pickups por round; skills usadas em momento legível (não spam).

## Log de ciclos

| Ciclo | Data | Item | Outcome | Métricas | Próximo |
|-------|------|------|---------|----------|---------|
| 0 | 2026-07-29 | Estrutura | Esqueleto `v1/` + `v1/renekton/` criado, 16 testes verdes | — | B1 |
| 1 | 2026-07-29 | B1 | `skillReady` alinhado ao formato real do WorldView (flat cooldowns, `skillsUnlocked`, recast do E, guarda de Dominus); suíte 79/79 | 12 testes renekton | B2 |
| 2 | 2026-07-29 | B2 | `updateBot` → `castAbility` (mesmo entrypoint humano); bundle `load-v1-bot.js` no build; CPU solo roda V1 com fallback baseline; suíte 80/80 | teste vm do encaminhamento + fallback | revisão Codex |
| 2b | 2026-07-29 | Revisão adversarial Codex | 4 achados maiores tratados: (1) crash com view nula — já coberto pelo B3; (2) **piloto×campeão**: política agora é recriada em `selectChampion2` e o piloto só entra quando o CPU joga de Renekton (sem módulo, V1 roda só o cérebro de arena — default P2 é Zed); (3) **`castAbility` não validava `stunned`** — guarda adicionada, vale para humano e bot pelo mesmo entrypoint; (4) teste vm reforçado (troca de campeão, stun). Menores: `Object.hasOwn` no mapa de slots; fallback exige as duas factories. Aceitos/documentados: strip por regex só suporta ESM simples; bundle V1 sem baseline não publica nada; freeze removido do global. Suíte 85/85 | testes novos de campeão/stun | B3 |
| 3 | 2026-07-29 | B3 | Harness headless `run-cpu-duels.mjs` (vm + stubs, P1 via teclado imitado, mulberry32 seeded em tudo, sem costura em `game/`); guarda de view nula no `think` do V1 (crash real quando o bot morre e o round segue vivo); suíte 85/85 | 100 partidas seed 42: win rate V1 **0%** (0V × 35 baseline × 65 empates por cap de frames); 641 rounds: 0V/41D/600E; sobrevivência à 1ª bomba **22%**; pickups/round **0**; skills disparadas **0** (kit nunca desbloqueia); round médio 72,4s. Diagnóstico raiz: o cérebro baseline trava permanentemente ao ser bloqueado no meio de uma célula (`nearCenter` nunca é alcançado → repete a última direção contra a parede); rounds estouram os 90s com poder empatado → empate; as 41 derrotas são auto-mortes na própria bomba | B4, com o stall de parede como bug prioritário a sanar junto |
| 4 | 2026-07-29 | Stall de parede | `unstickMovement` no V1 (querer andar sem progredir 0,45s → melhor alternativa segura, escrita na memória da arena para persistir entre thinks); baseline congelado como referência (D5); exports `worldFromCell`/`isBlocked`; suíte 88/88 | 100 partidas seed 42: win rate V1 **5%** (5V × 2 baseline × **93 empates**); sobrevivência à 1ª bomba **22% → 95%**; auto-mortes eliminadas (derrotas 41 → 2); round médio 76,6s; skills **0**; pickups/round 0,01. Novo gargalo: rounds estouram os 90s com poder empatado — o V1 não caça o rival travado nem coleta pickups (sem pathfinding), e `resolveTimeout` empata por poder idêntico | B5 (pathfinding: caçar rival + buscar pickups viram vitória no timeout); B4 depois |
| 5 | 2026-07-29 | Pathfinding BFS | `navigate-arena.mjs` (BFS 4-direções + `nextStepToward`, funções puras); navegação por rota quando o objetivo não é escape: orbs de skill próprios → pickup alcançável mais próximo por distância de caminho → célula do rival; passo persiste em `arenaMemory.lastDx/lastDz` + commit curto (0,05s — o commit longo do unstick mataria o `plantBomb` do baseline); `route`/`targetCell` preenchidos na v1-memory; ordem no think: plan → navigate → unstick (stall físico vence o frame); correção de assert latente no verify-cpu-duels (win rate arredondado); suíte 99/99 | 100 partidas seed 42: win rate V1 **5% → 12%** (12V × 2 baseline × **86 empates**); 603 rounds: 13V/3D/587E; pickups/round 0,01 → **0,055**; skills disparadas **0 → 2** (E — kit destravando via orbs); sobrevivência à 1ª bomba 95% → 92,7%; round médio 76,5s; timeouts 100. Diagnóstico: com rota livre o V1 converte empates em vitórias, mas 86% dos rounds ainda estouram os 90s com poder empatado — pickups e rival ficam atrás de crates (`grid 2` bloqueia o BFS) e o V1 não prioriza abrir caminho até o alvo; perseguição colada ao rival também anda na corda bamba do blast sem escape tempo-expandido | B5 (escape tempo-expandido); depois priorizar crate no caminho do alvo |
| 6 | 2026-07-29 | Abrir rota bombardeando crate | `open-route.mjs` (funções puras): `findRouteCrate` — BFS duplo (campo walkable do self + campo crate-passável do alvo), crate que minimiza dist(self→stand) + dist(alvo→crate), com `standCell` = vizinho livre mais próximo do self; `hasBombEscape` — simula a bomba hipotética (fuse 2,35, range do self, passOwners próprio) e exige célula alcançável com `dangerAt == 0` em `dist × s/célula + 0,35s slack < fuse` (s/célula = tile/speed do WorldView, fallback 0,45s); `canPlantRouteBomb` — mesmas travas do baseline (roundAge > 1,4, célula segura, slot de bomba). `navigateObjective`: sem alvo alcançável → `openRouteObjective` (prioridade orb própria > pickup > rival): fora do standCell navega até ele; no crate planta com fuga provada (`lastBombReason = "open-route"`, `targetCell` = crate, `route` até o standCell); sem fuga, recua um passo e espera. Bundle `package-v1-bot.mjs` + 6 testes novos; suíte **105/105** | 100 partidas seed 42: **timeouts 100 → 0** e empates de match **86 → 0**; 427 rounds: 25V/300D/102E; pickups/round 0,055 → **2,65** (~48×); skills disparadas 2 → **372** (Q33/W24/E315 — kit destrava toda partida); round médio 76,5s → **16,9s**; sobrevivência à 1ª bomba 92,7% → 87,5%. **Regressão: win rate 12% → 0%** (0V × 100 baseline). Diagnóstico: o gargalo do timeout está morto — o V1 abre o mapa, coleta e destrava o kit —, mas ao derrubar o muro ele entrega o combate aberto: persegue o rival colado sem escape tempo-expandido e morre cedo para as bombas de pressão do baseline (rounds de 17s). A defesa do V1 não acompanhou o ataque novo | B4/B5 viram bloqueadores: mapa de perigo temporal + escape tempo-expandido antes de mais agressão; depois re-tunar quando engajar o rival |
| 7 | 2026-07-30 | B4+B5 — percepção temporal de perigo | `danger-timeline.mjs` (puro): envelope `{ deadlyFrom, deadlyUntil }` por célula — blast 0,58s (`run-champion-bomb-duel.js:2449`), fuse 2,35 (`:731`), cadeia `other.age = other.fuse` + 1 frame (`:2455`, `:2412-2424`); janelas sobrepostas fundidas (conservador, documentado); `isDeadlyAt`/`safeWindowAfter`/`crossingSurvivable`; `escapePlan` — Dijkstra em (célula, chegada) com espera em célula segura, refúgio = célula segura para sempre, fallback "menos pior" (maior janela) e "hold" (primeiro passo só seguro depois → {0,0}); `safestNeighborStep`. Escape temporal assume TODO frame de perigo (`escapeTemporalDanger`, após `navigateObjective`): o scoring binário do baseline não tem gradiente em corredor. `nextStepToward`: rejeita binária → guarda temporal (atravessa lane fresca quando a travessia cabe; senão `safestNeighborStep`). Prova de fuga temporal para TODO plant: `hasTemporalBombEscape` (escapePlan com bomba hipotética deve achar refúgio) no gate do open-route + `vetoBombWithoutEscape` para plants do baseline. Correções medidas no harness: (1) passo de escape alinhado ao eixo (`escapeStepIntent`) — o passo cardeal cru enroscava no canto da própria bomba (jogo revoga `passOwners` ao sair, `:2042-2046`); (2) `memory.unstickHold` — os planners apagavam o commit de 0,6s do unstick 1 frame depois. Diagnóstico por instrumentação (script temporário, deletado): 84/90 mortes eram bombas open-route próprias (deriva pós-plant do baseline para bolso selado); depois plants-armadilha aprovados pela prova binária (janela temporal selava a saída). Suíte **116/116** | 100 partidas seed 42: **win rate 0% → 100%** (0V×100B → **100V×0B**); rounds 25V/300D/102E → **300V/12D/100E**; sobrevivência à 1ª bomba 87,5% → **100%**; pickups/round 2,65 → **9,52**; skills 372 → **2239** (Q460/W342/E1437); round médio 16,9s → 53,4s (rounds longos: o V1 sobrevive); timeouts 0. Honestidade: vitória sobre o baseline apenas — rounds de 53s indicam que o V1 mata devagar (joga seguro demais?); envelope fundido recusa escapes por brechas entre explosões (conservador); R nunca disparado | B6 (combos Renekton + quando engajar) e re-tunar agressão; B8 (pesos próprios) depois |
| 8 | 2026-07-30 | B6 — combos Renekton + Dominus | Recast do E com propósito (dice-through/finish/exit), combo E→W→Q dirigido por comboStep, Dominus no engage comprometido (1×/combo, fallback defensivo <40%), disciplina de mira (holdForFacing: segura o cast 1 tick até o facing apontar; facing vem de lastDx/lastDz do frame anterior, D8), hesitação curta dentro do combo (0,22–0,38s) vs normal fora (0,9–1,3s); revisão adversarial Codex encontrou 6 achados maiores (Dice preemptando combo, escape com facing velho, aimPending sem prazo, combo sem confirmação de cast, W em perigo, testes fabricando estados) → virou o ciclo 9; suíte 125/125 | 100 partidas seed 42: **100V×0B** mantido; rounds 300V/16D/102E; sobrevivência 1ª bomba **100%**; pickups/round 9,6; skills Q467/W360/E1356/**R53** (R: 0→53); round médio 53,4s → **50,8s** | ciclo 9 (correções da revisão) |
| 9 | 2026-07-30 | B6 — correções da revisão adversarial do ciclo 8 | Os 6 achados maiores + 3 menores já estavam implementados no HEAD consolidado ao iniciar o ciclo (a fonte `artifacts/codex-review-b6.md` não existe no repo — trabalho guiado pelo briefing); o ciclo verificou cada correção contra o achado e cobriu com a suíte: (1) **Dice não preempta o combo** — continuação (Dominus→W→Q) avaliada ANTES do gasto do Dice; o Dice só toma o frame como finalizador (`killCloses`), saída sem follow-up (W e Q em cooldown) ou último recurso com a janela fechando (`DICE_LAST_CALL` 0,8s); (2) **testes de combo realistas** — `renektonDashRecast > 0` entre todos os passos E→R→W→Q (Slice conectado sempre abre a janela) e nova ordem W/Q antes do Dice coberta; (3) **escape-dash exige facing seguro** — alinhado a vizinho com `dangerAt == 0` ou ao 1º hop do `escapePlan`; desalinhado segura o cast (null) e o escape temporal prepara o facing; fallback documentado: janela mortal < `ESCAPE_LAST_CALL` (0,4s) dasha torto; (4) **`aimPending` com prazo funcional** — `{ slot, until, holds }`, hold nunca estendido, após `AIM_MAX_HOLDS` (2) expirações do mesmo slot a avaliação desiste do cast uma vez e segue o pipeline; (5) **reconciliação de cast** — no início do `evaluateSkill`, ask não confirmado pelo WorldView (cooldown/Dominus/recast não moveram) reverte o snapshot de combo guardado em `lastSkill.effects` e repete o passo; blind spot documentado: Dice rejeitado dentro da janela é indistinguível de um disparado; (6) **W/Q ofensivos só com `danger === 0`** — o W teleporta e invalidaria a rota temporal calculada da posição antiga; defensivos (E alinhado, R `survive-low-health`) seguem legais em perigo. Menores: `resetCombo` limpa `aimPending`; Q sem recast conclui o combo na hora (`resetCombo`, sem estado residual até o timeout); Dice fora de combo reseta ANTES do ask → hesitação deliberada (0,9–1,3s). Suíte **139/139** | 10 partidas seed 42: **10V×0B** (win rate 100%; rounds 30V/6D/11E; skills Q32/W27/E139/R12; sobrevivência 1ª bomba 100%; timeouts 0); 10 partidas seed 7: **9V×1B** (rounds 28V/9D/12E; skills Q32/W24/E140/R14; sobrevivência 100%; timeouts 0) — 1 derrota sem baseline prévio de seed 7 para atribuir variância; R > 0 nos dois seeds. NÃO rodadas as 100 partidas (fica com o supervisor) | Supervisor registra o ciclo 8 e roda as 100 partidas; depois B7 (modelo do rival) |
| 10 | 2026-07-30 | B7 — modelo do rival | `read-rival.mjs` (puro, constantes `RIVAL_*`): heat map de ocupação por célula (+= dt por think, meia-vida 20s); rotas de fuga — track abre quando bomba com ≥0,8s de fuse cai a ≤2 manhattan do rival (qualquer dono), fecha na janela de 1,2s (a 3,45 u/s uma janela de 2s cobre 5+ células e estoura o clamp de 4 — descartaria exatamente as fugas reais) ou na explosão (só conta ≥0,5s; deslocamento zero NÃO vira hábito — medido no harness: tracks abertos à beira do fuse gravavam só "0,0" e anulavam a predição); hábito de bomba (célula com decay, adjacência a crate, alinhamento com o V1, sem dupla contagem por id). `memory.rivalModel` NÃO é limpo no `resetV1Memory` (documentado no campo; nasce com a política — D9). Uso tático: `chooseRouteTarget`/`chooseBlockedTarget` miram `predictRivalCell` (rival ameaçado + hábito → célula atual + offset favorito; senão lead de velocidade de 1,0s com fallback de meia-janela e heat; sem dados → célula atual — regressão preservada); plant corta-fuga em `planArenaActions` (`bombCutsRivalEscape`: blast da bomba hipotética cobre o rival E o destino favorito de fuga, ≤3 manhattan, `canPlantRouteBomb` + `hasTemporalBombEscape`, `lastBombReason = "cut-escape"`; `vetoBombWithoutEscape` segue provando todo plant). Piloto renekton sem mudança (desvio justificado: o facing vem do movimento, D8 — a navegação mirando a célula prevista já aponta o E na direção da interceptação). Diagnóstico com bundle instrumentado (script temporário, deletado após o uso): contra o baseline o modelo é INERTE por dado real — o rival passa a maior parte do tempo travado (stall de parede do ciclo 3), 8 tracks em 3 partidas e TODOS com deslocamento zero ("não foge" é o hábito real dele); `intercepts`/`cutEscapeTrue` = 0, logo os contadores do sanity ficam bit-idênticos ao ciclo 9. Suíte **150/150** | 10 partidas seed 42: **10V×0B** (rounds 30V/6D/11E; skills Q32/W27/E139/R12; sobrevivência 1ª bomba 100%; round médio **49,63s**; timeouts 0); 10 partidas seed 7: **9V×1B** (rounds 28V/9D/12E; skills Q32/W24/E140/R14; sobrevivência 100%; round médio **45,86s**; timeouts 0). Round médio abaixo de ~51s nos dois seeds; o modelo não ajuda nem atrapalha contra este oponente (medido, não assumido) — o valor aparece contra rivais móveis. NÃO rodadas as 100 partidas (fica com o supervisor) | Supervisor roda as 100 partidas; B8 (personalidade/self-play — primeiro adversário móvel para o modelo do rival) |
| 11 | 2026-07-30 | B8 — self-play + primeira personalidade (agressão) | **Self-play no harness**: flag `--opponent v1` (default `baseline` preservado) — P1 vira um segundo `createV1Policy()` SEM módulo de campeão (cérebro de arena puro: navegação BFS, open-route, escape temporal, veto, unstick), jogando de Katarina sem kit como o baseline, pelo mesmo caminho de teclado imitado, com stream de RNG próprio; relatório ganha `opponent.policy` dinâmico e `v1.aggression`; flag `--aggression` (0..1) injeta `personality` no P2 — a policy E o piloto renekton recebem a própria cópia (fábricas independentes). **Personalidade** (`v1/personality.mjs`): eixo `aggression` (0..1, default `AGGRESSION_DEFAULT` = 0,5 = comportamento pré-B8 exato; na navegação, abaixo do neutro equivale ao neutro — o rival já é o último alvo). Dois pontos tocados: (a) prioridade de alvo na navegação (`chooseRouteTarget`/`chooseBlockedTarget`: acima do neutro o rival previsto por `predictRivalCell` ganha do pickup quando a distância de caminho/manhattan do rival ≤ pickup + slack, slack = (a−0,5)×2×8 tiles — 0 no neutro, gate fechado, bit-idêntico; orbs de skill próprios seguem soberanos); (b) gatilho de engage do Renekton (branch 6: reach = 4,5 × (0,8 + 0,4a) → 3,6 a 5,4, exato em 0,5; o Dominus defensivo de `survive-low-health` e TODOS os branches de escape mantêm as constantes base — testado). Ponto (c) (bomba de pressão) NÃO implementado — desvio justificado: não existe gate de chance no caminho de plant do V1 (o corta-fuga do B7 é determinístico; as chances 0,18/0,26 vivem no baseline congelado, intocável por D5) e a regra do ciclo proíbe inventar comportamento novo de plant. Fuga temporal (`escapeTemporalDanger`, `vetoBombWithoutEscape`, `hasTemporalBombEscape`) NÃO lê personalidade — segurança soberana. Testes novos em `verify-personality.test.mjs` (10): clamp/limites 0 e 1, slack zero no neutro, prioridade de navegação nos dois temperamentos, engage reach em 0/0,5/1, Dominus defensivo inalterado, regressão bit-idêntica (policy sem `personality` ≡ default explícito em 120 thinks variados; harness 2 partidas sem `--aggression` ≡ `--aggression 0.5` campo a campo), self-play determinístico + campos do relatório. `package-v1-bot.mjs` inline do módulo novo; README do módulo atualizado. Suíte **160/160** | **Tuning self-play (10 partidas seed 42, `--opponent v1`)**: sem personality: **5V×5D** (rounds 19V/20D/10E; round médio **27,28s**; skills Q52/W30/E80/R10; pickups/round 5,61; sobrevivência 1ª bomba 100%; timeouts 0) — campo de treino real: win rate 100%→50% e round médio 49,63s→27,28s vs baseline; `--aggression 0.5` reproduz esses números bit-idêntico (medido). **0,5: 5V×5D** (27,28s) / **0,65: 3V×7D** (15V/23D/10E; 26,05s; pickups 5,06) / **0,8: 2V×8D** (13V/25D/10E; 25,93s; pickups 5,17) — agressão cega PERDE no self-play: caçar além do pickup sacrifica a economia e engaja num rival móvel que também escapa temporalmente; rounds encurtam ~1s mas contra o P2. **Default escolhido: 0,5 (neutro)** por win rate (0,5 × 0,3 × 0,2); legibilidade estável nos três valores (sem spam: Q/W/R estáveis, E dominante como dash, como sempre). Sanity modo baseline bit-idêntico ao ciclo 10 nos dois seeds: seed 42 **10V×0B** (30V/6D/11E; Q32/W27/E139/R12; 49,63s), seed 7 **9V×1B** (28V/9D/12E; Q32/W24/E140/R14; 45,86s). Self-play determinístico (duas rodadas idênticas, diff vazio) | Agressão CONDICIONADA em vez de global (engajar só com vantagem: rival encurralado/sem escape, vida baixa, poder maior) — o dado mostrou que agressão cega é punida por rival móvel; eixos risco/ganância de pickup; modelo do rival agora tem dados reais (rival móvel) para validar interceptação e corta-fuga no self-play |
| 12 | 2026-07-30 | B8+ — agressão CONDICIONADA (score de vantagem) | `v1/advantage.mjs` (puro, constantes `ADV_*`): `advantageScore(view, memory)` 0..1 — base 0,5 (luta equilibrada) + razão de vida (±0,25 por gap self−rival) + fury (+0,10 linear até 50, empower disponível) + gap de skills destravadas (±0,05/skill, clamp ±2; kit desconhecido em QUALQUER lado conta neutro — nunca lido como rival fraco) + rival encurralado (+0,10 × fração dos 4 vizinhos não seguros para o rival) + rival sob relógio de bomba (+0,15 × proximidade da janela mortal via `dangerTimeline`/`safeWindowAfter`, janela = fuse 2,35) + Dominus ativo (+0,10); sinais espaciais miram `predictRivalCell` quando `memory.rivalModel` existe (interceptação), senão a célula atual. `advantageThreshold(a)` = 0,55 + (0,5−a)×0,5 → 0→0,80 (quase nunca), 0,5→0,55 (vantagem clara), 1→0,30 (magra). Gate único `advantageEngageAllowed` em DOIS pontos: branch 6 do engage E (`renekton-skills`, avaliado por último no `&&`) e prioridade rival-vs-pickup (`chooseRouteTarget`/`chooseBlockedTarget`, após o curto-circuito do slack). SEM `personality` o gate abre sempre → bit-idêntico ao pré-ciclo-12 (regressão medida abaixo). Fuga temporal/veto/prova de escape NÃO leem vantagem — segurança soberana. Módulo registrado no bundle (`package-v1-bot.mjs`, após `danger-timeline`). **Tuning medido no mirror** (10 partidas, seed 42, `--opponent v1`): pré-gate reconfirmado (0,5→5×5 com 19V/20D/10E; 0,8→2×8); config A (base 0,55/slope 0,5): 0,1→2×8, 0,3→4×6, **0,5→5×5** (18V/20D/10E), 0,8→2×8 (bit-idêntico ao pré-gate — threshold 0,40 nunca veda: luta equilibrada = 0,5); config B (base 0,60/slope 0,2, acima da linha do empate em todo temperamento): 0,5→4×6, 0,8→1×9 — PIOR: os engages/caces que ela vedava não eram as derrotas. **Config A escolhida**: empata o melhor mirror medido (5×5) e veda engage em desvantagem real sem custo. Lição honesta: no mirror o gate é quase inerte — o V1 já engaja quase sempre com vantagem presente; o valor é o guard-rail comportamental (agressivo só com edge), não win-rate neste espelho. Teste de paridade null-vs-explícito do ciclo 11 substituído por determinismo do null (contrato mudou por design: neutro explícito agora exige vantagem — comportamento novo, não regressão). 13 testes novos em `verify-advantage.test.mjs` (sinais sobem/descem, gate bloqueia/libera, threshold cai com agressão, navegação condicionada nos dois sentidos, clamp [0,1]); header do `personality.mjs` atualizado para o contrato novo; suíte **173/173** | Mirror config A 0,5: 5×5 (18V/20D/10E, 27,77s) = melhor medido; vs baseline 10 partidas: seed 42 default **10V×0B** (30V/6D/11E, 49,63s, Q32/W27/E139/R12 — bit-idêntico ao ciclo 11) e `--aggression 0.5` também bit-idêntico (score ≥ 0,55 em todo engage vs baseline — dominância de vida); seed 7 default **9V×1B** (45,86s) e 0,5 idêntico (9V×1B, 45,86s). Sobrevivência à 1ª bomba 100% em todos | calibrar pesos com corpus maior de seeds; medir vantagem no plant corta-fuga (B7) no self-play |
| 13 | 2026-07-30 | Regressão 89×11 — wedge do escape dash | **Diagnóstico** (script temporário instrumentado, deletado): certificação 100 partidas seed 42 reproduzida exata (89V×11B; rounds 281V/96D/112E; skills Q282/W229/E1256/R126); as 96 derrotas em rounds são 96/96 mortes por bomba PRÓPRIA com `lastBombReason = "open-route"` e `objective = "escape"`, 94/96 com unstick ativo (querer andar sem progredir), 0 por skill/timeout. Trace frame-a-frame (9/9 mortes amostradas): plant open-route → dash E de escape → posição CONGELADA até o fim do fuse → morte. Mecanismo: o sweep do `castRenektonE` (game) só testa o CENTRO do corpo contra células; o dash para um passo (0,24) antes do sólido e a caixa de colisão (raio 0,3, testada pelo `moveEntity` a cada passo) fica sobreposta ao sólido — todo movimento posterior é rejeitado (wedge absoluto). **Isolamento por variantes de bundle** (fontes patchadas em memória, produção intacta): ciclo 10 desligado (`predict`+`cut-escape`) = bit-idêntico (89×11, 96 mortes); `reconcile`+`danger-gate` = bit-idêntico; `aim-holds` = 87×13 (ruído de RNG); **`escape-facing` desligado = 100×0** (17 mortes — nível pré-ciclo-9). Causa raiz: a exigência de facing seguro do ciclo 9 (revisão B6) alinha o dash ao 1º hop da rota de fuga e o dash percorre até 2,75 tiles pelo corredor até um sólido, aterrissando em wedge; no ciclo 8 o facing velho apontava de volta para o crate recém-aberto e o dash atravessava para espaço livre. **Correção** (intenção da revisão preservada — nada de dash em facing velho): `dashLandingFree` no branch de escape — `sliceDashLanding` espelha o sweep do jogo (crates passam, qualquer bomba para, 2,75/3,15 tiles) e `dashBodyBlocked` espelha a colisão do `moveEntity` (4 cantos raio 0,3 + bombas não atravessáveis); pouso em wedge SEGURA o cast como facing desalinhado (o escape temporal anda a rota provada); `closing` (<0,4s) e o fallback "none" (sem direção segura conhecida) mantêm o dash torto — um pouso congelado fora do blast ainda vence ficar parado dentro dele. 5 testes novos (wedge segura o dash, pouso livre dispara, sweep para antes do sólido, crates passam/bomba para, caixa de colisão). Suíte **178/178** | Antes: 100 partidas seed 42 **89V×11B** (96 mortes na própria bomba open-route). Depois: 30 partidas seed 42 **30V×0B** (rounds 90V/1D/34E; 2 mortes residuais por wedge fora do branch de escape — dash de ENGAGE (branch 6) e fallback "none"/closing — documentadas, não são a regressão); sanity 10 partidas **10×0** seed 42 (30V/0D/11E) e **10×0** seed 7 (30V/1D/12E); sobrevivência à 1ª bomba 100% em todos | as 100 oficiais ficam com o supervisor; residual documentado: se as 100 mostrarem wedges fora do escape, avaliar `dashLandingFree` também no engage (branch 6) e no Dice ofensivo |
| 13b | 2026-07-30 | Certificação oficial do ciclo 13 (supervisor) | 100 partidas seed 42 pós-fix wedge do escape: **100V×0B** (restaurado de 89×11); rounds 300V/3D/117E; sobrevivência 1ª bomba 100%; pickups/round 9,94; skills Q392/W309/E1363/R164; round médio 50,2s | ciclo 14 (residual de wedges) |
| 14 | 2026-07-30 | Residual do ciclo 13 — wedges fora do escape (engage + Dice ofensivo) | `dashLandingFree` estendido aos dois caminhos de dash que restavam sem gate: engage (branch 6, `close-distance`) e Dice ofensivo (`dice-through`/`dice-finish`). Semântica do jogo confirmada antes de codar (`castRenektonE`, `run-champion-bomb-duel.js:1480`): o sweep para em sólido (crate quebra e deixa passar) e em qualquer bomba; o rival NÃO bloqueia — o acerto é por distância ao segmento (≤0,74) e o dash atravessa o rival, logo o pouso planejado pode ficar ATRÁS do alvo. **Política de pouso por branch** (documentada no header de `renekton-skills.mjs`): (a) escape — inalterado, exceções last-call do ciclo 13 preservadas (`closing` <0,4s e `"none"` aceitam pouso em wedge: congelado fora do blast vence parado dentro dele); (b) engage — SEMPRE gateado, sem caso last-call: o engage nasce em célula segura e sem relógio, segurar custa zero (a navegação anda até o melee — wedge no engage é suicídio posicional sem nenhuma compensação); (c) Dice ofensivo — pouso SEMPRE gateado, mesmo com a janela fechando: `recastClosing` sobrepõe só o FACING, nunca o pouso — janela expirada deixa o bot parado na célula segura, wedge pode congelá-lo no aberto ao lado do rival, logo a janela apodrece. A assimetria é por princípio: pouso ruim só é aceito quando a alternativa é pior que um possível congelamento. `dice-exit` ficou sem gate (desvio justificado: não estava no residual; dasha de volta para a célula de origem do engage, recém-ocupada pelo próprio bot). 5 testes novos (engage wedge segura como aim/disciplina com `comboStep` intacto; engage com pouso livre dispara; `dice-through` wedge segura; `dice-finish` wedge segura mesmo com a kill disponível; last-call com wedge apodrece a janela). Regressão dos caminhos do ciclo 13 mantida pelos testes originais. Suíte **183/183** | 30 partidas seed 42: **30V×0B**, rounds **90V/0D/34E** — residual de 2 mortes → **0**; sanity 10 partidas **10×0** seed 42 (30V/0D/11E) e **10×0** seed 7 (30V/0D/12E — a 1 derrota do ciclo 13 na seed 7 também sumiu); sobrevivência à 1ª bomba 100% em todos; skills 30 partidas Q154/W128/E356/R22; round médio 48,7s | as 100 oficiais ficam com o supervisor; observação: se alguma morte futura por wedge aparecer, o único dash ainda sem gate é o `dice-exit` |
| 15 | 2026-07-30 | Rounds empatados — phantom do harness + wedge permanente do próprio V1 | **Diagnóstico** (script temporário instrumentado, deletado): dos 111 "rounds empatados" da certificação seed 42, **100 eram fantasma do harness** — `prevRound` era inicializado ANTES de `match.start()`, e o `startRound()` inicial (round 0→1) fechava um round vazio com winner null no frame 1 de cada partida; só **11 eram empates reais por timeout** (poder empatado aos 90s). Correção em `run-cpu-duels.mjs` (`prevRound` depois de `start()`): todo `drawnRounds` histórico estava inflado em 1 por partida (os "117E em 420" do ciclo 13 eram ~17 reais; os "34E" do ciclo 14 eram 4 reais). Trace frame-a-frame dos 11 reais: em TODOS o V1 passa 80+s **wedgeado contra a parede da borda** (pos ~(6.48,-5.88), caixa de colisão raio 0,3 sobre a linha 0) depois de um dash de escape disparado pela exceção last-call do ciclo 13 (`"none"`/`closing`): o dash salvou do blast e congelou o bot para o resto do round (todo `moveEntity` rejeitado; unstick cicla sem efeito; rival 100% hp, alcançável 0%, habilidades 0 — poder empata aos 90s). A hipótese do briefing ("rival enclausurado e V1 não abre rota") foi FALSIFICADA: `findRouteCrate` achava rota em 18/18 sondas e o V1 é quem estava paralisado. **Siege tentado e REVERTIDO por medição**: priorizar a rota até o rival selado aos 30s converteu 7 vitórias de timeout em empates (11→18 draws) — parar de farmar entrega o empate de poder. **Correção**: `unwedgeMovement` em `plan-arena-actions.mjs` — freeze watch (zero progresso por 1,2s com intent de movimento OU caixa sobre sólido; frames de intent `{0,0}` do baseline não resetam mais o relógio — padrão medido no trace), frames de perigo ficam com o escape temporal (gate `dangerAt > 0`), E pronto do WorldView, direção = maior dash cardeal cujo pouso NÃO fica congelado (teste espelha `moveEntity`: congelado = as 4 destinações cardeais bloqueadas — sobreposição pura não basta, o passo que se AFASTA da célula é aceito; crates cruzados pelo sweep contam como abertos porque o jogo os destrói, `castRenektonE:1498`), steering de facing por 1 frame e `intent.skill = "e"`; só com módulo de campeão (o P1 kitless do self-play não tem com o que se recuperar). 4 testes novos (steer+fogo, cooldown segura, nunca dispara em progresso/espera no aberto, frames idle não resetam). Suíte **187/187** | 30 partidas seed 42: **30V×0B**, rounds **90V/1D/0E** (ciclo 14: 90V/0D/34E) — empates reais 4→**0**; sanity 10 partidas: seed 42 **10×0** (30V/1D/0E) e seed 7 **10×0** (30V/0D/0E); self-play 10: **7V×3D** (exigido ≥ 4×6); diagnóstico focado nas 11 partidas que tinham draw: **11 → 0** empates, 11×0 matches; pickups/round 14,2; round médio 62,3s. Custo honesto: 1 derrota de round apareceu em seed 42 (round que antes empatava por timeout agora resolve contra — o bot descongelado se expõe; matches seguem 100%) | certificação 100 partidas do ciclo 15 (supervisor); investigar a 1 derrota de round em seed 42 |
| 14b | 2026-07-30 | Certificações oficiais do ciclo 14 (supervisor) | Baseline 100 partidas seed 42: **100V×0B, 0 derrotas em rounds** (300V/0D/110E; sobrevivência 100%; pickups/round 9,99; skills Q502/W404/E1163/R73; round médio 49,0s). Primeira certificação self-play 100 partidas: **60V×40B** (rounds 231V/187D/104E; round médio 32,2s) — o espelho sem kit perde para o kit Renekton mas vence 40%; novo baseline de treino | ciclo 15 (rounds empatados) |
