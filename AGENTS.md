# Regra única

Coloque regras e apresentação da partida em `game/`, conteúdo de um campeão em `champions/<nome>/`, preparação de modelos em `champions/prepare-playable-models/` e material didático em `architecture-course/`; só crie outro módulo quando o histórico mostrar um novo grupo de arquivos mudando junto.


## STOP: headless Playwright (user 2026-07-25)

See STOP-HEADLESS-PLAYWRIGHT.md. Do not run hostile-ui Playwright scripts or spawn headless_shell without explicit user permission.

